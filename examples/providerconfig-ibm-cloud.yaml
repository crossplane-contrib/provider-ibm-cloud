apiVersion: ibmcloud.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: ibm-cloud
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: provider-ibm-cloud-secret
      key: credentials
status:
  refreshToken:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: provider-ibm-refresh-token
      key: credentials 
   